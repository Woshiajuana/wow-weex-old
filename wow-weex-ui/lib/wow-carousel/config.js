
import config                       from '../../config'
import Assign                       from '../../assign'
export default Assign({
    arrow_style: {
        width: 20,
        height: 20,
        marginRight: 32,
        borderColor: '#979797',
        transform: 'rotate(45deg)',
    }
}, config)
